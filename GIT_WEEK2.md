# 📚 สรุปบทเรียน Week 2: โลกคู่ขนาน (Branching & Merging)

## 🎯 คอนเซปต์: แยกทำ แล้วค่อยรวม
ในสัปดาห์นี้ เราเรียนรู้การทำงานแบบไม่กระทบโค้ดหลัก (`main`) โดยการสร้าง "ทางเบี่ยง" (`branch`) ออกไปทำงานจนเสร็จ แล้วค่อยนำกลับมารวม (`merge`)

---

## 🛠️ ขั้นตอนที่ทำในสัปดาห์นี้ (Step-by-Step)

1.  **สร้าง Branch ใหม่:** แยกตัวออกจาก `main` เพื่อไปทำฟีเจอร์ Profile
2.  **ทำงานใน Branch:** สร้างและแก้ไขไฟล์ `Profile.md` (ตอนทำ `main` จะไม่เห็นไฟล์นี้)
3.  **กลับสู่ Main:** ย้ายตัวกลับมาถนนหลักเพื่อเตรียมรับงาน
4.  **รวมร่าง (Merge):** สั่งดึงข้อมูลจาก Branch ลูก เข้ามารวมกับ `main`
5.  **Push:** อัปเดตข้อมูลล่าสุดขึ้น GitHub

---

## 💻 เจาะลึกคำสั่ง Git (Command Breakdown)

### 1. `git checkout -b feature/my-profile`
* **หน้าที่:** สร้าง Branch ใหม่ชื่อ `feature/my-profile` และย้ายเข้าไปทันที
* **ความหมาย:** เหมือน Copy งานไปทำในห้องส่วนตัว ปลอดภัยจากงานหลัก

### 2. `git checkout main`
* **หน้าที่:** สลับกลับมาที่ Branch หลัก
* **สิ่งที่ต้องระวัง:** ไฟล์ที่ทำใน Branch อื่นจะหายไปชั่วคราว (เพราะใน main ยังไม่มี) ต้อง Merge ก่อนถึงจะมา

### 3. `git merge feature/my-profile`
* **หน้าที่:** รวม Branch (ดึงงานจากลูกมาใส่แม่)
* **Note:** ต้องยืนอยู่ที่ `main` (ตัวรับ) ก่อนสั่งคำสั่งนี้

### 4. `git push`
* **หน้าที่:** อัปเดต GitHub
* **ผลลัพธ์:** ส่งสถานะล่าสุด (ที่มี Profile แล้ว) ขึ้นไปเก็บ

---

## 📊 ภาพจำลอง (Diagram)

**Before Merge:**
(main)      O
             \
(feature)     O --- O (เพิ่ม Profile)

**After Merge (Fast-forward):**
(main)              O (ย้ายตามมาล่าสุด)
                   /
(feature)         O