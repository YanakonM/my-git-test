# ✅ สรุป Week 1 — พื้นฐาน Git + GitHub + VS Code

## 🎯 เป้าหมายสัปดาห์แรก
1. สร้าง Git repository บนเครื่องตัวเอง
2. สร้าง GitHub repository
3. เชื่อมเครื่อง → GitHub
4. commit + push ไฟล์ขึ้น GitHub ได้จริง

---

## 🟢 สิ่งที่ทำสำเร็จ

### 1) เริ่มต้น Git (Init)
- สร้างโฟลเดอร์ `my-git-project-test`
- ใช้คำสั่ง `git init` เพื่อเริ่มระบบบันทึกประวัติ

### 2) การบันทึกงาน (Commit)
- ใช้ `git status` เช็คดูไฟล์
- ใช้ `git add .` เพื่อเตรียมไฟล์ทั้งหมด
- ใช้ `git commit -m "ข้อความ"` เพื่อบันทึกงาน (Snapshot)

### 3) เชื่อมต่อ GitHub (Remote)
- สร้าง Repo บนเว็บ GitHub
- ใช้คำสั่งเชื่อมต่อ:
  `git remote add origin https://github.com/YanakonM/my-git-test.git`
- เปลี่ยนชื่อ branch หลัก: `git branch -M main`

### 4) อัปโหลด (Push)
- ส่งงานขึ้นเว็บ: `git push -u origin main`

---

## 🟦 สรุปคำสั่งที่ใช้บ่อย
- `git init`: เริ่มโปรเจค
- `git status`: เช็คสถานะ
- `git add .`: เลือกทุกไฟล์
- `git commit -m "..."`: บันทึก
- `git push`: ส่งขึ้น GitHub