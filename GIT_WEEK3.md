# 🤝 สรุปบทเรียน Week 3: การทำงานร่วมกัน (Collaboration & Pull Request)

## 🎯 คอนเซปต์: ขออนุญาตก่อนรวม (Pull Request)
ในการทำงานจริง เราจะไม่ Merge โค้ดเองในเครื่อง แต่จะส่ง Branch ขึ้นไปบน GitHub แล้วเปิด **Pull Request (PR)** เพื่อให้ทีมช่วยตรวจสอบโค้ดก่อนที่จะอนุมัติให้รวมเข้า `main`

---

## 🛠️ วงจรการทำงานแบบทีม (The Collaboration Cycle)

### 1. เริ่มต้นงาน (Start)
* `git checkout -b feature/team-page`: สร้าง Branch ใหม่เพื่อทำงานเฉพาะส่วน
* สร้างและแก้ไขโค้ดตามปกติ
* `git commit -m "..."`: บันทึกงานในเครื่อง

### 2. ส่งงาน (Push & PR)
* `git push -u origin feature/team-page`: ส่ง Branch ขึ้นไปบน GitHub
* **Open Pull Request:** กดปุ่มสร้าง PR บนหน้าเว็บ GitHub เพื่อขอรวมโค้ด

### 3. ตรวจและรวม (Review & Merge)
* **Review:** ทีมเข้ามาดูความเปลี่ยนแปลง ตรวจสอบความถูกต้อง
* **Merge pull request:** กดปุ่มสีเขียวบนเว็บเพื่อรวมร่าง (Merge) เข้าสู่ `main`
* **Delete branch:** ลบ Branch ที่เสร็จแล้วทิ้งไป

### 4. ดึงงานกลับ (Update Local)
* `git checkout main`: กลับมาที่เครื่องเรา
* `git pull`: ดึงโค้ดล่าสุด (ที่เพิ่ง Merge บนเว็บ) ลงมาที่เครื่องเรา
* *ผลลัพธ์:* เครื่องเราจะมีไฟล์ครบเหมือนบนเว็บเป๊ะๆ

---

## 🔑 คำสั่งสำคัญ (Key Commands)

* **`git push -u origin <branch>`**: ส่ง Branch และตั้งค่าการเชื่อมต่อครั้งแรก
* **`git pull`**: ดาวน์โหลดความเปลี่ยนแปลงล่าสุดจาก Server ลงมาทับเครื่องเรา (ใช้บ่อยมากก่อนเริ่มงานใหม่)

---

## 📊 ภาพจำลอง (Diagram)

1. Local (Mick)  ----> Push ----> GitHub (Review)
                                      |
                                    Merge (Click Button)
                                      |
2. GitHub (Main) ----> Pull ----> Local (Main)