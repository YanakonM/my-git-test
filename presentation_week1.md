---
marp: true
theme: default
paginate: true
backgroundColor: #f0f4f8
---

# 🚀 สรุปการเรียนรู้ Git สัปดาห์ที่ 1
## Git & GitHub Foundations (Week 1 Summary)

**ภารกิจ:** การตั้งค่าพื้นฐาน, การทำ Version Control ในเครื่อง และการเชื่อมต่อขึ้น Cloud

**จัดทำโดย:** [นายญาณากร]

---

## 🎯 เป้าหมายของสัปดาห์แรก (The Goal)

**สิ่งที่ต้องการบรรลุ:**
* เข้าใจการทำงานพื้นฐานของ Git (Local)
* สามารถเชื่อมต่อโค้ดจากเครื่องตัวเองขึ้นสู่ GitHub (Remote) ได้
* สร้าง Repository แรกที่มีการ Commit ไฟล์ Readme และเอกสารสรุปความรู้

**ภาพประกอบ:**


---

## ⚙️ การเริ่มต้นตั้งค่า (Setup & Initialization)

**ขั้นตอนที่ทำ:**
1.  **Config:** ตั้งค่าชื่อผู้ใช้และอีเมล (`git config`) เพื่อระบุตัวตนคนทำ
2.  **Init:** เปลี่ยนโฟลเดอร์โปรเจคธรรมดาให้กลายเป็น Git Repository (`git init`)

**ภาพประกอบ:**


---

## 📝 กระบวนการบันทึกงาน (The Basic Workflow)

**หัวใจสำคัญของ Git:**
* **Add (Staging):** เลือกไฟล์ที่ต้องการเตรียมบันทึก (`git add .`)
* **Commit (Snapshot):** บันทึกเวอร์ชันอย่างเป็นทางการพร้อมข้อความกำกับ (`git commit -m "..."`)

**ภาพประกอบ:**


---

## 🛠️ การแก้ปัญหาที่พบ (Troubleshooting)

**ปัญหาที่เจอ:**
* แจ้งเตือน Error: `remote origin already exists` และ `Repository not found` ตอนพยายามเชื่อมต่อ GitHub

**วิธีแก้ไข:**
* ใช้คำสั่ง `git remote remove origin` เพื่อลบการเชื่อมต่อเก่าที่ผิดพลาดออกก่อน แล้วค่อยเชื่อมต่อใหม่ให้ถูกต้อง

**ภาพประกอบ:**


---

## 💡 บทเรียนสำคัญ: อย่าลืม Save ไฟล์!

**เหตุการณ์:**
* พยายาม Commit ไฟล์เอกสาร แต่ Git แจ้งว่า "nothing to commit"

**สาเหตุและการแก้ไข:**
* **สาเหตุ:** สร้างไฟล์ใน VS Code แล้วแต่ **ลืมกด Save (บันทึก)** ทำให้ Git มองไม่เห็น
* **แก้ไข:** กด Save ไฟล์ (Ctrl+S) แล้วสั่ง add/commit ใหม่อีกครั้งก็สำเร็จ

**ภาพประกอบ:**


---

## 🎉 ความสำเร็จในสัปดาห์แรก (Final Success)

**ผลลัพธ์ที่ได้:**
* ✅ โค้ดทั้งหมดถูกอัปโหลด (Push) ขึ้นสู่ GitHub เรียบร้อย
* ✅ มีไฟล์เอกสารครบถ้วนทั้ง `readme.md` และ `GIT_BASIC1.md`
* ✅ เข้าใจกระบวนการพื้นฐานครบวงจร (Init -> Add -> Commit -> Push)

**ภาพประกอบ:**


---

## 📚 สรุปสิ่งที่ได้เรียนรู้ (Key Takeaways)

* **Git vs GitHub:** แยกออกว่า Git คือเครื่องมือในเครื่อง GitHub คือที่เก็บของออนไลน์
* **Workflow:** เข้าใจสถานะของไฟล์ (Working -> Staging -> Committed)
* **Attention to Detail:** เรียนรู้ว่าเรื่องเล็กน้อยอย่างการ "ลืม Save ไฟล์" หรือ "พิมพ์ URL ผิด" ก็ทำให้งานสะดุดได้ แต่ก็แก้ไขได้ไม่ยาก
* **Next Step:** มีพื้นฐานที่แน่นพอสำหรับการเรียนรู้เรื่อง Branching ในสัปดาห์ที่ 2!